Установка Sass == npm install -g sass(в терминале, требуется node.js)
Запустить компиляцию в css файл == sass app\styles\main.scss  build/stylesheets/index.css

Команда для слежки за изминениями == sass app\styles\main.scss  build/stylesheets/index.css --watch